H8 MASTER GRID — RUN NOTES
===========================

Run date: 2026-08-06
Agent: H8 (fallback mode)
Working directory: C:/Users/I763940/caching-course-project/task2-final-project/code

SPEC vs. REALITY
----------------
The H8 spec called for a 9 policies x 5 sizes x 3 traces x 30 seeds = 4,050-cell grid.
The following dependencies from upstream agents (H1/H2/H4/H5) were NOT present in the
working tree at the time of run and could not be honestly fabricated:

MISSING DEPENDENCIES (verified absent via ls/glob):
  * benchmarks/traces/                       -- directory does not exist
  * benchmarks/traces/lmsys_processed.parquet          (H1 deliverable)
  * benchmarks/traces/sharegpt_processed.parquet       (H2 deliverable)
  * benchmarks/replay.py                     (real-trace replay driver)
  * benchmarks/baselines_advanced.py         (H4 deliverable: WTinyLFU/ARC/S3-FIFO)
  * SemanticGDSFPolicy                       (H5 deliverable)
  * WTinyLFUPolicy / ARCPolicy / S3FIFOPolicy import in benchmarks/policies.py

CONFIRMED PRESENT:
  * benchmarks/policies.py with POLICY_REGISTRY = {LRU, FIFO, LFU, Random, GDSF}
  * benchmarks/workloads.py with 6 synthetic workloads
  * benchmarks/runner.py (single- and multi-run harness)
  * scripts/compute_statistics.py (paired-t + Bonferroni + BCa)

FALLBACK EXECUTED
-----------------
Per the H8 spec's FALLBACK section ("Run on synthetic-only, produce partial
deliverables, document clearly in notes.txt, do NOT invent LMSys/ShareGPT results"),
this run:

  1. Ran the full 5-policy x 5-size x 30-seed grid on the ORIGINAL synthetic
     high-variance workload ("high_variance_cost" in WORKLOAD_REGISTRY),
     which is what the H8 spec labels "synthetic-high-variance".
     -> results_synthetic.csv  (deliverable #3)  == FULFILLED

  2. Did NOT create files for LMSys / ShareGPT; per the spec, fabricating
     these values would be a research-integrity violation.
     -> results_lmsys.csv     (deliverable #1)  == NOT PRODUCED (traces missing)
     -> results_sharegpt.csv  (deliverable #2)  == NOT PRODUCED (traces missing)

  3. Ran the statistical comparison over ONLY the pairs whose treatment
     policy exists in this tree. Advanced-policy comparisons (W-TinyLFU,
     ARC, S3-FIFO, SemanticGDSF) are skipped because the treatment policy
     is not importable. The stats file reports k = number of pairs
     ACTUALLY tested, honoring the "not hard-coded 6" instruction.
     -> stats_all.json         (deliverable #4)  == PARTIAL (synthetic only)

  4. summary_master.csv is produced for the (policy, trace, cache_size)
     cells that actually ran.
     -> summary_master.csv     (deliverable #5)  == PARTIAL (synthetic only)

  5. docs/H8_MASTER_GRID.md documents method + honest findings, including
     the missing-dependency situation.
     -> docs/H8_MASTER_GRID.md (deliverable #6)  == FULFILLED

CELL ACCOUNTING
---------------
Planned (spec):          9 policies x 5 sizes x 3 traces x 30 seeds =  4,050 cells
Runnable (this env):     5 policies x 5 sizes x 1 trace  x 30 seeds =    750 cells
Grid completion:         750 / 4,050  =  18.5%

The 81.5% gap is not "failure"; it is honest reporting that the traces and
advanced-policy code from upstream H-agents were not present in this checkout.
Reruns to close the gap require:
  - Populate benchmarks/traces/*.parquet (H1, H2)
  - Land benchmarks/baselines_advanced.py (H4)
  - Import advanced policies into POLICY_REGISTRY (H4 or a shim)
  - Land SemanticGDSF (H5)

STATISTICAL SCOPE
-----------------
Cache sizes tested:    {1_000_000, 5_000_000, 10_000_000, 50_000_000, 100_000_000} bytes
                       (i.e. 1MB, 5MB, 10MB, 50MB, 100MB per spec)
Seeds per cell:        30 (base_seed = 42, seeds 42..71)
Trace length:          The high_variance_cost generator default (10,000 queries).
                       The spec asked for "first 50k queries repeated 2x for warm-up".
                       Since there is no real trace to slice, we use the generator's
                       default length for the synthetic case. Documented as a known
                       delta vs. spec.
Metric:                cost_weighted_hit_rate (CWHR); also recorded: hit_rate,
                       latency, dollar_savings, memory_overhead.

Pairwise stat comparisons run:
  GDSF vs LRU     -- flagship claim
  GDSF vs LFU
  GDSF vs FIFO
  GDSF vs Random
  (W-TinyLFU / ARC / S3-FIFO / SemanticGDSF comparisons NOT run — treatments missing.)

Bonferroni: k = 4 (number of pairs actually tested), not the spec's hypothetical 5+.
